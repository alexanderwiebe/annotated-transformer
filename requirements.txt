--find-links https://download.pytorch.org/whl/torch_stable.html

# Minimal required pins for legacy Annotated Transformer (Python 3.10)
# Core computational stack (must stay aligned)
numpy==1.22.4
pandas==1.3.5
torch==1.11.0
torchtext==0.12.0
torchdata==0.3.0
spacy==3.2.0

# Loosely constrained / optional tools (allow safe upgrades)
altair>=4.1,<5
jupytext>=1.13,<2
flake8
black
GPUtil
wandb
